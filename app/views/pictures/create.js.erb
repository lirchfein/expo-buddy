function addPicture() {
  HTML = '<%= j render "components/thumnail_pic", pic: Picture.all.last.pic %>'
  const picsDiv = document.getElementById('pic-div');
  picsDiv.insertAdjacentHTML('afterbegin', HTML);
}

addPicture()
